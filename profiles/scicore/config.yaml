jobs: 4
latency-wait: 60
printshellcmds: true
keep-going: true
rerun-incomplete: true

# Snakemake >=8 uses executor plugins (not --cluster).
executor: cluster-generic
cluster-generic-submit-cmd: "sbatch"
cluster-generic-cancel-cmd: "scancel"
jobscript: "profiles/scicore/jobscript.sh"
